# `@aura/config`

Future home for narrowly shared configuration primitives. Services will continue to own and validate their configuration; this package will not become a global configuration singleton. No abstraction is needed in Phase 1.
