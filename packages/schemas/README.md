# `@aura/schemas`

Future home for shared, versionable validation contracts at TypeScript service boundaries. Zod schemas will be added when real API contracts exist; Phase 1 deliberately defines none.
