# Kafka infrastructure

Kafka will carry asynchronous, versioned domain events. Broker, topic, producer, and consumer configuration is intentionally deferred. Realtime audio must never use this path.
